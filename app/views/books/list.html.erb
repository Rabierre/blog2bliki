<table>
    <tr>
        <th>All Books</th>
    </tr>

    <% @books.each do |book| %>
        <tr>
            <td><%= book.title %></td>
            <td><%= book.author %></td>
            <td><%= link_to 'Show', book_path(book) %></td>
        </tr>
        <!-- 여기에 줄 긋기 -->
    <% end %>
</table>
</div>
